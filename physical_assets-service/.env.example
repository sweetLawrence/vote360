# Supabase — use service role key (bypasses RLS)
SUPABASE_URL=https://<project>.supabase.co
SUPABASE_SERVICE_KEY=<service_role_jwt>

# Shared secret — Telegram bot sends this as x-bot-secret header
BOT_SECRET=your_bot_secret_here

# Internal reconciliation engine URL (Docker network name)
RECONCILIATION_URL=http://reconciliation:3004/internal/reconcile

PORT=3001
